{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // Test and check runners import source directly through `tsx`, which resolves the real `.ts`
    // files; only safe alongside `noEmit`.
    "allowImportingTsExtensions": true,
    "declaration": false,
    "declarationMap": false,
    "sourceMap": false,
    "rootDir": "."
  },
  "include": ["tests/**/*.ts", "checks/**/*.ts", "scripts/e2e/**/*.ts"],
  "exclude": ["node_modules"]
}
